{
  "name": "job-scraper",
  "module": "src/index.ts",
  "scripts": {
    "scrape": "bun run src/index.ts",
    "scrape:dev": "DEV=true bun run src/index.ts",
    "test": "bunx playwright test",
    "test:headed": "bunx playwright test --headed",
    "test:ui": "bunx playwright test --ui",
    "test:debug": "bunx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/bun": "^1.3.9",
    "@types/node": "^25.3.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "type": "module",
  "dependencies": {
    "playwright": "^1.58.2",
    "xlsx": "^0.18.5",
    "yaml": "^2.8.2"
  }
}
